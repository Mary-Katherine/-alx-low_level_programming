#include <stdio.h>

/**
 * main - this shows the function of my main
 *
 * Return: it is 0 always after success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
